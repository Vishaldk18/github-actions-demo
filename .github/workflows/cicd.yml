name: cicd-pipeline
on:
 workflow_dispatch:
   
jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: pull code
        run: echo "pull code from github" 
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build code
        run: echo "building code"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test-code
        run: echo "test-code"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy-code
        run: echo "deploy-code"
