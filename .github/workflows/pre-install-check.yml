name: pre-check
on:
  workflow_dispatch:
jobs:
  ubuntu:
    runs-on: ubuntu-latest
    steps:
      version-print:
        - name: versions
          run: |
            docker --version
            git --version
            node --version
            python --version
